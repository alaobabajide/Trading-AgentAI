[project]
name = "trading-agent"
version = "0.1.0"
description = "Hybrid AI trading agent — stocks + crypto"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
